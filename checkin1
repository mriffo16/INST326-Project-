import random 

category = ["Movies", "TV Shows", "Pop Culture"] 
point_value = ["100", "200", "300"]

""" def category():
    while True:
        print("Select a category: Movies, TV Shows, or Music")
        category = input("Category: ").capitalize()

        if category in ["Movies", "TV Shows", "Music"]:
            break
        else:
            print("Invalid category. Please select Movies, TV Shows, or Music.")

    while True:
        print("Select point value: 100, 200, or 300")
        point_value = input("Point value: ")

        if point_value in ["100", "200", "300"]:
            break
        else:
            print("Invalid point value. Please select 100, 200, or 300.")

    return category, point_value
"""
#Function to display the points each player currently has
def display_pointboard(self):
        print("POINTS:")
        for player, score in self.points.items():
            print(f"Player {player}: {points}")

def select_question():
    selected_category = random.choice(categories)
    selected_point_value = random.choice(point_values)
    question = questions[selected_category][int(selected_point_value)]
    return question

questions = {
    "Movies": {
        100: "If you watch the Marvel movies in chronological order, which movie would you start with?",
        200: "What was the first feature-length animated movie ever released?",
        300: "What is the first animated movie made entirely by CGI (computer-generated imagery)?"
    },
    "TV Shows": {
        100: "What is the highest-rated television show on IMDB?",
        200: "Spongebob Squarepants was originally going to be named what?",
        300: "What is the longest-running game show of all time?"
    },
    "Music": {
        100: "Who performed at the Super Bowl halftime show in 2021?",
        200: "What country is Rihanna from?",
        300: "Before becoming a solo artist, Beyoncé was part of Destiny’s Child group. Name the two other members."
    }
}

class Jeopardy:
    def __init__(self):
        self.scores = {1: 0, 2: 0}
        self.current_player = 1

    def display_pointboard(self):
        print("POINTS:")
        for player, score in self.scores.items():
            print(f"Player {player}: {score}")

   def winner(self):
    max_score = -1
    winner = None
    for player, score in self.scores.items():
        if score > max_score:
            max_score = score
            winner = player
    print(f"Player {winner} wins with a score of {max_score}!")

